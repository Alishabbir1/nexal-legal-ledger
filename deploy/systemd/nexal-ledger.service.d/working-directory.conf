# systemd drop-in — production Ledger WorkingDirectory.
# Service runs as User=nexal; code lives at /opt/nexal-ledger (NOT /root/).
# Matches bank statement upload deploy: cd /opt/nexal-ledger && git pull && systemctl restart

[Service]
WorkingDirectory=/opt/nexal-ledger
