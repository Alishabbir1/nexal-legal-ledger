# systemd drop-in — production Ledger WorkingDirectory.
# gunicorn runs as user sync; code must live under /opt/, NOT /root/.
# Matches PHASE4B deployment: cd /opt/nexal-legal-ledger && git pull && systemctl restart

[Service]
WorkingDirectory=/opt/nexal-legal-ledger
